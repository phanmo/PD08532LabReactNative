import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
    container: {
        borderRadius: 10,
        alignItems: 'center',
        paddingVertical: 10,
        marginVertical: 10,
        width: 150,

    },

    btnText:{
        fontSize: 18,
        fontWeight: 'bold',
       
    }
});
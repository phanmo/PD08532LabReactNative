import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
    container: {
        borderRadius: 100,
        alignItems: 'center',
        paddingVertical: 7,
        width: 350,
        marginVertical: 10,
        borderWidth: 3,
        borderColor: '#396929',
    },

    btnText:{
        fontSize: 25,
        fontWeight: 'bold',
       
    }
});